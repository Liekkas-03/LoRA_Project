# Cloud GPU training dependencies. Install this on AutoDL or another GPU host,
# not on the local lightweight workspace.

accelerate
bitsandbytes
datasets
math-verify[antlr4_13_2]
peft
protobuf
pyyaml
sentencepiece
sympy
tensorboard
transformers
trl

# Optional for later serving/demo on the cloud:
# gradio
# vllm
